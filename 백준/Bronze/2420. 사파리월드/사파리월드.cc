#include <iostream>
#include <string>
#include <cstdlib>

using namespace std;

int main(){
    long long int a, b;
    cin >> a >> b;
    long long int d = abs(a - b);
    cout << d;
    return 0; 
}