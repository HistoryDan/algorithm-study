# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
N,K = map(int, input().split())
nums = [int(i) for i in input().split()]

cnt = 0
for num in nums:
	if str(K) not in str(num): cnt += 1
	
print(cnt)